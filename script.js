// student object
const student = {
    name: 'John Doe',
    age: 22,
    enrolled: true,
    courses: ['Math', 'Science', 'History'],

    // Method to display basic info
    displayInfo() {
        console.log(`Name: ${this.name}, Age: ${this.age}`);
    }
};